{
  "name": "topp-signal-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio topp-signal",
    "create-local": "graph create --node http://localhost:8020/ topp-signal-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ topp-signal-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 topp-signal-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.50.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.2"
  }
}
